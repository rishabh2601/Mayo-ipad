<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.cnmc.painclinic.painreport"
    android:versionCode="1"
    android:versionName="1.0" >

    <supports-screens android:largeScreens="true" android:normalScreens="true" android:smallScreens="true" android:resizeable="true" android:anyDensity="true" />
    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="10" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"></uses-permission>    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:configChanges="orientation|keyboardHidden"
            android:name="org.cnmc.painclinic.painreport.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.cnmc.painclinic.painreport.Browselocal"
            android:configChanges="orientation|screenSize"
            android:label="PainSurvey"
        />
        <activity
            android:name="org.cnmc.painclinic.painreport.PainReportSettingsActivity"
            android:configChanges="orientation|screenSize"
            android:label="Pain Report Settings"
            />
        <receiver  android:name=".TimeAlarm">
            <!-- for now we are skipping this. To get an alarm you have to run the app and its settings
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
            -->
        </receiver>
        <service android:enabled="true" 
            android:name="org.cnmc.painclinic.painreport.PainReportService"            
            />
    </application>
</manifest>

